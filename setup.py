from setuptools import find_packages, setup
setup(name="minicons",
      version="0.1",
      platforms=["any"],  # or more specific, e.g. "win32", "cygwin", "osx"
      packages=['minicons'],
      )
